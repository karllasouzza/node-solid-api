version: "3"

services:
  api-solid-pg:
    image: bitnami/postgresql:latest
    build: .
    ports:
      - "5232:5232"
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=apisolid
